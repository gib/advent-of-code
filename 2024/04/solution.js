// --- Day 4: Ceres Search  ---
import { readFileSync } from 'fs';

// const inputPath = "./test-case.txt";
const inputPath = "./input.txt";

const rows = readFileSync(inputPath, 'utf-8').split(/\r?\n/);
const rowCount = rows.length;
const colCount = lines[0].length;

// Part 1
// Find "XMAS" and "SAMX" in the grid
const needle = ['X', 'M', 'A', 'S'];
const eldeen = ['S', 'A', 'M', 'X'];
const checkMatch = (row, column) => {
    
}



console.log(`Part 1: `);

// Part 2
console.log(`Part 2: `);
